jwt {
  issuer   = "http://localhost:8080"
  audience = "momento-clients-dev"
  secret   = ${?JWT_SECRET}   # or: "dev-secret"
  accessTtlSeconds = 900
  refreshTtlSeconds = 2592000
}

ktor {
  application { modules = [ ApplicationKt.module ] }
}
