ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }

  application {
    modules = [ "ApplicationKt.module" ]
  }
}

jwt {
  issuer = "momento-backend"
  audience = "momento-users"
  secret = "change-this-to-a-long-random-secret"
  accessTtlSeconds = 900
  refreshTtlSeconds = 2592000
}